export const metadata = {
  title: 'Metrics Dashboard | Urban Shopfronts',
  robots: { index: false, follow: false },
};

export default function MetricsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
